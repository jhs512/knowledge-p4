# 로컬 커스텀 타입

표준 16종으로 부족할 때 이 vault 전용 타입을 여기에 등록합니다.
등록된 타입의 노드는 `custom/` 폴더에 두고, frontmatter는 `type: custom` + `custom_type: <이름>`을 사용합니다.

| custom_type | 정의 | 도입일 | 비고 |
|---|---|---|---|
| _(아직 없음)_ | | | |
